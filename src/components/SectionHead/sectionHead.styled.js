import styled from "styled-components";

export const StyledSectionHead = styled.div`
  h3 {
    font-weight: 800;
    font-size: 22px;
    display: inline;
    padding: 2px 10px;
    box-shadow: rgba(0, 0, 0, 0.2) 1.95px 1.95px 2.6px;
    border-left: 3px solid var(--es-identity-color);
  }
`;
